<UserControl x:Class="Wacton.Japangolin.Sentences.UI.Mains.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:cal="http://www.caliburnproject.org"
        xmlns:mains="clr-namespace:Wacton.Japangolin.Sentences.UI.Mains"
        xmlns:domain="clr-namespace:Wacton.Japangolin.Sentences.Domain.Mains;assembly=Wacton.Japangolin.Sentences.Domain"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=mains:DesignTimeMainViewModel, IsDesignTimeCreatable=True}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <ListBox x:Name="ListView" Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="3" 
                  FontSize="12" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="10" ItemsSource="{Binding GolinEnglish}" BorderThickness="0">
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel IsItemsHost="True" Orientation="Horizontal" />
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding EnglishConjugated}" ToolTip="{Binding KanaBase}"/>
                </DataTemplate>
            </ListBox.ItemTemplate>
            <ListBox.ItemContainerStyle>
                <Style BasedOn="{StaticResource MetroListBoxItem}" TargetType="ListBoxItem" >
                    <Setter Property="Padding" Value="2" />

                    <!-- 
                        overwriting ListBoxItem.IsEnabled according to whether there is Japanese translation for it 
                        then updating the text color according to the enabled state
                    
                        note: intellisense seems to think this binding is on the data context of this view (MainViewModel instead of the Translation items)
                    -->
                    <Style.Triggers>
                        
                        <DataTrigger Binding="{Binding IsTranslatable}" Value="False" d:DataContext="{d:DesignInstance Type=domain:IGolin}">
                            <Setter Property="IsEnabled" Value="False"/>
                        </DataTrigger>

                        <Trigger Property="IsEnabled" Value="True">
                            <Setter Property="Foreground" Value="Orange"/>
                        </Trigger>

                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground" Value="{DynamicResource BlackBrush}"/>
                        </Trigger>

                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Foreground" Value="Magenta"/>
                        </Trigger>

                        <MultiDataTrigger d:DataContext="{d:DesignInstance Type=domain:IGolin}">
                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding Path=IsTranslatable}" Value="True" />
                                <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=IsMouseOver}" Value="True" />
                            </MultiDataTrigger.Conditions>
                            
                            <Setter Property="Foreground" Value="Cyan" />
                        </MultiDataTrigger>
                    </Style.Triggers>
                </Style>
            </ListBox.ItemContainerStyle>
        </ListBox>

        <TextBlock Grid.Row="3" Grid.Column="1" Grid.ColumnSpan="3" Text="{Binding KanaSentence}" 
                   FontSize="12" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="10"/>
        
        <TextBlock Grid.Row="4" Grid.Column="1" Grid.ColumnSpan="3" Text="{Binding KanjiSentence}" 
                   FontSize="12" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="10"/>

        <Button Grid.Row="5" Grid.Column="1" Grid.ColumnSpan="3" Width="100" Height="50" FontSize="12" HorizontalAlignment="Left" cal:Message.Attach="NextSentence" Content="Next"/>
    </Grid>
</UserControl>
